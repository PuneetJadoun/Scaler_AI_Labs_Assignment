# PII Redaction Tool - runtime dependencies
#
# Install with:
#   pip install -r requirements.txt
#
# Presidio's NER detector also needs a spaCy language model, which is a
# model download, not a pip package, and so is intentionally NOT listed
# below. After installing this file, run:
#   python -m spacy download en_core_web_lg

Faker
presidio-analyzer
python-docx
spacy
